<Project>

  <!-- tests/plugins/ is a separate directory tree, so MSBuild does not auto-import the plugins/ props.
       Pull in the same strict policy here so the test plugins build under identical rules. -->
  <Import Project="$(MSBuildThisFileDirectory)../../plugins/Directory.Build.props" />

</Project>
