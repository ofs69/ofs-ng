#pragma once

namespace ofs {

// Severity of a user-facing notification. Drives the footer bell badge tint and the per-item glyph.
enum class NotifyLevel { Info, Success, Warning, Error };

} // namespace ofs
