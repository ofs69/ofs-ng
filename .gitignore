# Build output
bin/
build*/
cmake-build-*/
**/obj/
managed/plugins/Ofs.Core/Generated/

# Transient translation work-orders from `tools/translations.py todo` (see docs/TRANSLATING.md).
tools/localization/*.batch.toml

# Generated theme preview artifacts
data/themes/preview.html
data/themes/palette.json

# IDE / editor
.idea/
.vs/

# Python
__pycache__/

# OS cruft (Windows)
Thumbs.db
[Dd]esktop.ini
$RECYCLE.BIN/
*.lnk

# External wiki mirrors cloned under docs/ (each carries its own .git) — reference only, not ours.
docs/imgui.wiki/
docs/imgui_test_engine.wiki/

# DocFX generated output (docs/apidocs/). The api/*.md landing pages are tracked; the extracted
# metadata (api/*.yml) and the rendered site are build artifacts. (obj/ covered above.)
docs/apidocs/_site/
docs/apidocs/api/*.yml
docs/apidocs/api/.manifest
