# API Reference

The full public surface of the `Ofs.Api` assembly. Types are grouped in the table of contents to
the left; everything here is generated from the assembly's XML documentation comments.

Start with `Ofs.OfsPlugin` (the plugin base class) and `Ofs.IOfsHost` (the host surface).
